// An Object in JavaScript is a collection of key-value pairs, where each key is a string (or Symbol) and each value can be any data type.



// <==__ Object __==>

let bioData = {
    funllName: "Js Coder",
    editor: "Vs Code",
    language: "JavaScript"
}
console.log(bioData, typeof bioData);

// For Single Value 
console.log(bioData.funllName);
console.log(bioData.editor);
console.log(bioData.language);

